# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TasksWithSQLite3' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TasksWithSQLite3
  pod 'FMDB'
  pod 'BEMCheckBox'

  target 'TasksWithSQLite3Tests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TasksWithSQLite3UITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
